services:
  - type: web
    name: bgmeta-hd-api
    runtime: python
    plan: free
    pythonVersion: "3.10.12"
    buildCommand: "pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
    startCommand: "python -m uvicorn main:app --host 0.0.0.0 --port 10000 --workers 1"

